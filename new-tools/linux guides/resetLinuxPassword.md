#Reset Password

+ After post bios, single press **esc**, to access the menu
+ Select **Advanced options**
+ Select **Recovery mode**
+ Select **root**
+ Remount root `mount -rw -o remount /`
+ Change password `passwd username`
+ 